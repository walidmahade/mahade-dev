# Nixpacks configuration for Railway deployment
# PHP application with built-in server

[phases.setup]
nixPkgs = ["php83"]

[phases.install]
cmds = []

[phases.build]
cmds = []

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t ."
